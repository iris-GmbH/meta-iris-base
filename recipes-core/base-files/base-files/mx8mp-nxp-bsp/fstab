/dev/root            /                    auto       defaults              1  1
proc                 /proc                proc       defaults              0  0
devpts               /dev/pts             devpts     mode=0620,ptmxmode=0666,gid=5      0  0
tmpfs                /run                 tmpfs      mode=0755,nodev,nosuid,strictatime 0  0

# The encrypted volumes /mnt/iris, /mnt/datastore, and /etc/counter are unlocked and mounted in initramfs.
# Because nfs boot is also supported and these devices shouldn't be mounted in this case.

# /var/volatile is mounted in the initramfs
# tmpfs                /var/volatile        tmpfs      defaults              0  0
